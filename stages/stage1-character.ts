/**
 * ============================================================================
 *                    《符文大陆传说》 - 阶段一：冒险者的诞生
 * ============================================================================
 * 
 * 📚 本阶段学习目标：
 *    1. 原始数据类型 (Primitive Data Types)
 *    2. 类型推论 (Type Inference)
 *    3. 内置对象 (Built-in Objects)
 * 
 * 📖 参考文档：https://ts.xcatliu.com/basics/primitive-data-types.html
 * ============================================================================
 *                           🎮 实战任务
 * ============================================================================
 * 
 * 任务：创建一个游戏角色，包含以下属性和功能
 * 
 * TODO 1: 使用原始数据类型定义角色的基础属性
 *         - playerName: string (角色名称)
 *         - level: number (等级，初始为1)
 *         - hp: number (生命值，初始为100)
 *         - mp: number (魔法值，初始为50)
 *         - attack: number (攻击力，初始为10)
 *         - defense: number (防御力，初始为5)
 *         - isAlive: boolean (是否存活，初始为true)
 *         - gold: number (金币，初始为0)
 * 
 * TODO 2: 使用类型推论，让 TypeScript 自动推断经验值类型
 *         - exp = 0
 *         - maxExp = 100
 * 
 * TODO 3: 使用内置对象 Date 记录角色创建时间
 *         - createdAt: Date
 * 
 * TODO 4: 创建一个显示角色信息的函数 (返回值为 void)
 *         - showPlayerInfo(): void
 * 
 * TODO 5: 创建一个角色升级的函数
 *         - levelUp(): void
 *         - 升级时：level+1, maxExp+50, hp+20, mp+10, attack+3, defense+2
 * 
 * TODO 6: 运行游戏，展示角色信息，然后升级一次，再次展示信息
 * 
 * 💡 提示：完成后运行 npm run stage1 或 npx ts-node stage1-character.ts 测试
 *
 * 【执行入口代码】
 * 下面这段请保留在文件中，用于运行演示
 */

console.log('='.repeat(50));
console.log('      欢迎来到《符文大陆传说》！');
console.log('='.repeat(50));

// 在这里调用你的函数来展示角色信息、升级、再展示信息
// ============================================================================
//                           参考答案（先自己尝试！）
// ============================================================================
// 
// 如果你完成了上面的任务，可以对照下面的参考答案检查
// 建议：先自己写完，遇到问题再看答案
// 
// 将下面的注释取消即可看到参考答案：
// 

/*
// TODO 1: 定义角色基础属性
let playerName: string = '勇者';
let level: number = 1;
let hp: number = 100;
let mp: number = 50;
let attack: number = 10;
let defense: number = 5;
let isAlive: boolean = true;
let gold: number = 0;

// TODO 2: 类型推论（TS会自动推断为number）
let exp = 0;
let maxExp = 100;

// TODO 3: 内置对象
let createdAt: Date = new Date();

// TODO 4: 显示角色信息
function showPlayerInfo(): void {
    console.log('\n📋 角色信息：');
    console.log(`  名称: ${playerName}`);
    console.log(`  等级: Lv.${level}`);
    console.log(`  HP: ${hp}`);
    console.log(`  MP: ${mp}`);
    console.log(`  攻击力: ${attack}`);
    console.log(`  防御力: ${defense}`);
    console.log(`  金币: ${gold}`);
    console.log(`  经验: ${exp}/${maxExp}`);
    console.log(`  状态: ${isAlive ? '存活' : '死亡'}`);
    console.log(`  创建时间: ${createdAt.toLocaleString()}`);
}

// TODO 5: 升级函数
function levelUp(): void {
    if (exp >= maxExp) {
        exp = exp - maxExp;
        level = level + 1;
        maxExp = maxExp + 50;
        hp = hp + 20;
        mp = mp + 10;
        attack = attack + 3;
        defense = defense + 2;
        console.log(`\n🎉 恭喜！${playerName} 升级到了 Lv.${level}！`);
    } else {
        console.log(`\n❌ 经验不足，还需要 ${maxExp - exp} 经验才能升级`);
    }
}

// TODO 6: 运行游戏
showPlayerInfo();

// 模拟获得经验并升级
exp = 100;
console.log('\n⚔️ 你击败了一只史莱姆，获得了100经验！');
levelUp();
showPlayerInfo();
*/
